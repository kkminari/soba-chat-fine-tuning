"""
코멘트 + 질문 리프레이징 데이터 생성 프롬프트
- 핵심 태스크: 사용자 응답 시뮬레이션 + 챗봇 코멘트 + 다음 질문 리프레이징
- 1 API 호출 = 3가지 변형 배치 생성
- 모델: GPT-4o
"""

SYSTEM_PROMPT = """당신은 마케팅 리서치 설문 챗봇의 학습 데이터를 생성하는 전문가입니다.

주어진 설문 컨텍스트에 대해, 다양한 사용자 응답을 시뮬레이션하고
각 응답에 대한 챗봇의 공감 코멘트와 다음 질문 리프레이징을 생성하세요.

챗봇 스타일 규칙:
- 존댓말 사용 (반말 금지)
- 이모지 금지
- 짧고 간결한 문장 (한 문장당 30자 이내)
- 마크다운 문법 사용 금지 (**, ##, ``` 등)
- 공감 코멘트는 1~2문장
- 다음 질문은 원본을 자연스럽게 대화체로 변환

출력은 반드시 JSON 배열로만 응답하세요. 다른 텍스트 없이 JSON만 출력."""

USER_TEMPLATE = """설문 컨텍스트:
- 주제: {topic}
- 타겟: {target_audience}
- 현재 질문: {current_question}
- 다음 질문 (원본): {next_question}
- 대화 단계: {stage}

다음 3가지 사용자 응답 스타일로 각각 시뮬레이션하고, 챗봇 응답을 생성하세요:

변형1: {variation_1}
변형2: {variation_2}
변형3: {variation_3}

출력 형식 (JSON 배열):
[
  {{
    "user_answer": "시뮬레이션된 사용자 응답",
    "comment": "챗봇 공감 코멘트 (1~2문장, 이모지 포함)",
    "next_question": "다음 질문을 대화체로 자연스럽게 리프레이징"
  }},
  ...
]"""

# 변형 조합: (응답스타일, 감정톤) 쌍
VARIATION_SETS = [
    # Set A: 일반적인 조합
    [
        "짧고 간결하게 1~2문장, 긍정적 태도",
        "3~5문장으로 구체적 예시 포함, 중립적 태도",
        "구어체(ㅋㅋ, ㅎㅎ)와 이모지 섞어서, 적극적 태도",
    ],
    # Set B: 도전적인 조합
    [
        "1~2문장 짧게, 부정적이거나 불만족스러운 태도",
        "상세하게 설명하되 무관심한 태도",
        "구어체로 친근하게, 중립적 태도",
    ],
    # Set C: 다양한 길이
    [
        "아주 짧게 한 문장, 긍정적 태도",
        "길고 상세하게 경험담 포함, 긍정적 태도",
        "짧게 2문장, 부정적 태도",
    ],
]

def get_variation_set(index: int) -> list[str]:
    """인덱스에 따라 변형 세트를 순환 반환"""
    return VARIATION_SETS[index % len(VARIATION_SETS)]

def get_stage(question_index: int) -> str:
    """질문 순서에 따른 대화 단계"""
    if question_index <= 1:
        return "초반 (1~2번째 질문) — 아직 어색한 분위기, 가벼운 톤"
    elif question_index <= 4:
        return "중반 (3~5번째 질문) — 자연스러워진 상태, 친근한 톤"
    else:
        return "후반 (6~8번째 질문) — 라포 형성, 깊이 있는 대화"
